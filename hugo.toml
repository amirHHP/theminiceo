baseURL = 'https://theminiceo.ir'
languageCode = 'fa-ir'
languagedirection = 'rtl'
title = 'TheMiniCEO'
theme = 'hextra'

[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML"]

# این خط باعث می‌شود آدرس‌ها از ریشه سایت شروع شوند
relativeURLs = false